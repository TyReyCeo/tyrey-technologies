"""TyRey Connect AI™ — communications + AI + CRM module.

Phase 1: two-way SMS from a unified inbox with AI drafts, contacts, usage
metering, and compliance (opt-out keywords, registration gating). All carrier
traffic goes through the provider adapter in `providers/` — demo mode by
default, Twilio when configured. See docs/connect-ai/ARCHITECTURE.md.
"""
